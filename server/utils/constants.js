const SUBSCRIPTION_PRICING = {
  monthly: 25,
  yearly: 250,
}

const PRIZE_POOL_PERCENT = 50

const PRIZE_SPLIT = {
  match5: 0.4,
  match4: 0.35,
  match3: 0.25,
}

export { SUBSCRIPTION_PRICING, PRIZE_POOL_PERCENT, PRIZE_SPLIT }
